info:
  name: health
  type: http
  seq: 4

http:
  method: GET
  url: http://127.0.0.1:8000/health
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
