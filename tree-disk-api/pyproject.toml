[tool.poetry]
name = "tree-disk-api"
version = "0.1.0"
description = ""
authors = ["Tony <tonymeissner70@gmail.com>"]
license = "MIT"
readme = "README.md"
package-mode = false

[tool.poetry.dependencies]
python = ">=3.10"
fastapi = "^0.115.4"
uvicorn = "^0.32.0"
gunicorn = "^23.0.0"
python-multipart = "^0.0.17"
tree-disk-segmentation = "^0.1.3"
tree-disk-pith = "^0.1.4"
tree-disk-rings = "^0.4.5" 
gdown = "^5.2.0"

[tool.poetry.group.dev.dependencies]
tree-disk-segmentation = { path = "../tree-disk-segmentation" }
tree-disk-pith = { path = "../tree-disk-pith" }
tree-disk-rings = { path = "../tree-disk-rings" }
pytest = "^8.3.3"
httpx = "^0.27.2"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
